#!/usr/bin/env ansible-playbook
---
- name: run the playbook tasks on the localhost
  hosts: 127.0.0.1
  connection: local
  become: yes
  tasks:
  - name: Install latest version of git i
    apt:
    name: git
    state: latest
